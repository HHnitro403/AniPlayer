using Aniplayer.Core.Interfaces;

namespace Aniplayer.Core.Services;

public class PlayerService : IPlayerService
{
    // Placeholder — playback logic lives in PlayerPage.axaml.cs (P/Invoke layer).
    // This will be populated if we extract a cross-platform abstraction later.
}
